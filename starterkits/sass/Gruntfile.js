module.exports = function (grunt) {

  grunt.registerTask('prepare-sass-starterkit', 'Copies all bootstrap-sass related files to related starterkits folders', function () {
    
  });

  //grunt.registerTask('install', ['prepare-sass-starterkit']);
};